{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-readonly",
  "description": "Read-only Kuviewer desktop shell permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
